services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: cot_admin
      POSTGRES_PASSWORD: cot_pass
      POSTGRES_DB: cotizaciones
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
